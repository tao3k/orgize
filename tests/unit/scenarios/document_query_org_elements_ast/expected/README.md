# Expected outcome

The query parses every admitted Org document into parser-owned elements, filters only that AST
projection, returns no matches, spawns no provider process, and stays within the hard gate.
