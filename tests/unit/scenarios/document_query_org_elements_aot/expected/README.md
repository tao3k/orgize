# Expected outcome

The query parses every admitted Org document into Scheme-AOT elements, filters only that graph
projection, returns no matches, spawns no provider process, and stays within the hard gate.
