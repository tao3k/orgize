* 20.04 Org Elements Alignment Audit

This audit pins the current =org-elements= bridge against upstream
=org-element.el= before using it as the durable substrate for =CONTRACT_ORG=.

The upstream reference was cloned into =.data/org-mode= and audited at:

#+begin_example
bzg/org-mode b470d81 2026-06-10 16:08:54 +0200
#+end_example

The reference constants were extracted with Emacs by loading
=.data/org-mode/lisp/org-element.el= and printing:

- =org-element-all-elements=
- =org-element-all-objects=
- =org-element-greater-elements=
- =org-element-recursive-objects=
- =org-element-affiliated-keywords=
- =org-element--standard-properties=

** Upstream Baseline

Upstream =org-element-all-elements= contains:

#+begin_example
babel-call center-block clock comment comment-block diary-sexp drawer
dynamic-block example-block export-block fixed-width footnote-definition
headline horizontal-rule inlinetask item keyword latex-environment
node-property paragraph plain-list planning property-drawer quote-block
section special-block src-block table table-row verse-block
#+end_example

Upstream =org-element-all-objects= contains:

#+begin_example
bold citation citation-reference code entity export-snippet footnote-reference
inline-babel-call inline-src-block italic line-break latex-fragment link macro
radio-target statistics-cookie strike-through subscript superscript table-cell
target timestamp underline verbatim
#+end_example

Upstream affiliated keywords are:

#+begin_example
CAPTION DATA HEADER HEADERS LABEL NAME PLOT RESNAME RESULT RESULTS SOURCE
SRCNAME TBLNAME
#+end_example

The standard Org property surface includes at least:

#+begin_example
:begin :post-affiliated :contents-begin :contents-end :end :post-blank
:secondary :mode :granularity :cached :org-element--cache-sync-key
:robust-begin :robust-end :true-level :buffer :deferred :structure :parent
#+end_example

** Current Bridge State

The current bridge already provides a real graph substrate:

- root record =org-data=
- stable numeric =id=
- =parentId= and =childIds=
- =Document::org_elements_graph()=
- graph traversal through =parent=, =children=, =descendants=, =ancestors=,
  =lineage=, and =subtree=
- typed queries over category, kind, context, outline path, summary predicates,
  property predicates, affiliated =NAME=, graph relations, and limit

Headline records are represented as =category= =section= and =kind=
=headline=. Headline body content is represented through a =kind= =section=
record, so body elements hang below =headline -> section=. Property drawers
are represented as =kind= =property-drawer= records, with =node-property=
records as their children. Planning lines are represented as =kind=
=planning= children of their headline when at least one of =SCHEDULED=,
=DEADLINE=, or =CLOSED= is present. Citation references are represented as
=kind= =citation-reference= children of their containing =citation=. Table rows
and cells are represented in the same graph.

Current property maps are useful but not yet =org-element-property=
compatible. They contain summary-derived keys plus headline-specific keys such
as =:raw-value=, =:title=, =:level=, =:true-level=, =:todo-keyword=,
=:todo-type=, =:priority=, =:tags=, =:scheduled=, =:deadline=, =:closed=, and
effective property drawer keys.

The first standard property layer is now projected into every record:
=:begin=, =:end=, =:post-affiliated=, =:contents-begin=,
=:contents-end=, =:post-blank=, and =:parent=. =:begin= and =:end= use the
same byte positions exposed by the JSON =source= annotation. =:parent= is the
stable parent record id, or =null= for the root. Headline records now use the
parser-owned body =section= range for =:contents-begin= and =:contents-end=;
the body =section= record also exposes its body range as content. Remaining
content boundaries stay =null= when the semantic model does not yet own an
exact =org-element.el=-compatible boundary. =:post-blank= remains =null= until
blank-line ownership is modeled per node kind.

Graph relations are first-class query AST now. =OrgElementsIndexQuery= can
filter with =child_of=, =descendant_of=, =ancestor_of=, and =at= predicates over
stable graph ids. =CONTRACT_ORG= relation conditions compile through the same
query AST after contract bindings resolve to graph ids.

** Gap Matrix

| Axis | Current bridge | Gap before complete alignment |
|------+----------------+-------------------------------|
| Kind set | Most parser-owned element/object kinds are emitted, including =section=, =planning=, =property-drawer=, and =citation-reference=. | =diary-sexp= is absent. |
| Org root | =org-data= exists as the document root and carries the shared standard property layer. | Root content/post-blank boundaries still need exact Org-compatible ownership. |
| Graph | Parent/child ids, traversal, and first-class relation predicates are available through =OrgElementsIndexQuery=. | Boolean query composition beyond conjunctive predicates is not modeled yet. |
| Affiliated keywords | Raw affiliated keyword records are emitted; structured affiliated property exposes =name=. | The full upstream affiliated keyword property surface is not structured yet. |
| Properties | Summary keys, headline properties, source positions, parent id, and parser-owned headline body content ranges are queryable through =property_eq= and =property_contains=. | Exact =:contents-begin=, =:contents-end=, and =:post-blank= semantics still need per-kind parser-owned boundaries; internal cache properties remain intentionally absent. |
| Org-specific extensions | =plain-text=, =unknown=, =cloze=, =table.el=, =target-definition=, and =footnote-entry= preserve orgize-owned facts. | These need explicit provider-owned extension status so callers do not confuse them with upstream Org element kinds. |

** Closeout Standard

Complete alignment means the =org-elements= bridge can answer the same class of
questions as an =org-element-map= plus =org-element-property= workflow without
forcing callers back to raw source reads.

The closeout should land in this order:

1. Add =diary-sexp= at the syntax and semantic model layer before adding its
   bridge record. This is a parser-surface gap, not a bridge-only omission.
2. Continue refining standard source-position properties so =:contents-begin=,
   =:contents-end=, and =:post-blank= match =org-element.el= per node kind.
3. Expand affiliated properties from =name= to the upstream keyword set while
   preserving raw affiliated keyword records.
4. Add boolean composition on top of the public query AST so conjunction,
   disjunction, and negation are shared by =CONTRACT_ORG=, JSON query, and
   future host bindings.
5. Mark orgize-only kinds as provider-owned extensions in the JSON contract and
   snapshot tests.

** Stronger Improvements After Alignment

After the compatibility surface is stable, remaining improvements should stay
semantic and contract-visible rather than ad hoc:

- widen the shared =OrgElementQueryPredicate= AST only through parser-owned
  predicates that can execute over =OrgElementGraph= records;
- keep =CONTRACT_ORG=, host JSON query packets, and Rust predicates compiling
  through the same predicate AST instead of adding caller-private string
  condition parsing;
- refine property provenance beyond the current =summary=, =standard=,
  =local=, and =inherited= labels only when a downstream consumer needs a more
  specific inheritance policy;
- keep orgize-owned nodes in explicit extension namespaces instead of mixing
  extensions such as =plain-text= into the upstream kind namespace silently.

The runtime alignment snapshot is
[[file:../../tests/integration/semantic_ast/snapshots/integration_test__semantic_ast__semantic_ast_projects_elements_bridge__semantic_ast_projects_org_element_alignment_gap_has_snapshot.snap][semantic_ast_projects_org_element_alignment_gap_has_snapshot]].
The split gap snapshots are
[[file:../../tests/integration/semantic_ast/snapshots/integration_test__semantic_ast__semantic_ast_projects_elements_bridge__org_element_kind_alignment.snap][org_element_kind_alignment]]
and
[[file:../../tests/integration/semantic_ast/snapshots/integration_test__semantic_ast__semantic_ast_projects_elements_bridge__org_element_standard_property_gap.snap][org_element_standard_property_gap]].
The real-document regression snapshot is
[[file:../../tests/integration/semantic_ast/snapshots/integration_test__semantic_ast__semantic_ast_projects_elements_bridge__semantic_ast_projects_org_guide_real_document_org_elements_regression_has_snapshot.snap][semantic_ast_projects_org_guide_real_document_org_elements_regression_has_snapshot]].

** P0-P7 Landing Evidence

- P0: the root gitlink and nested orgize checkout are treated as separate
  commit boundaries; root updates carry only the nested commit pointer.
- P1: =org-element.el= element, object, greater-element, recursive-object, and
  affiliated-keyword baselines are parsed from
  =.data/org-mode/lisp/org-element.el= and compared against checked-in
  constants. Element-like and object-like gaps are now empty except the
  intentional orgize-owned =plain-text= object extension.
- P2: alignment assertions are split into targeted snapshots for kind coverage
  and standard-property coverage, so upstream drift and intentional unmapped
  properties fail independently. =OrgElementQueryPredicate= is now the shared
  boolean predicate AST for graph-record conditions.
- P3: =CONTRACT_ORG= selectors now cover affiliated fields, summary fields,
  property fields, =or=, and =not= through the shared predicate AST in addition
  to count and relation conditions.
- P4: upstream =defconst= extraction is automated in the integration suite; a
  changed upstream list must update the checked-in baseline intentionally.
  =OrgElementsIndexKind= also marks orgize-owned extensions such as
  =plain-text= with the =orgize= extension namespace.
- P5: =.data/org-mode/doc/org-guide.org= is parsed as a real document
  regression and snapshots record count, selected kind counts, and first
  top-level headlines. Index records now carry property provenance so callers
  can distinguish standard, local, summary-derived, and inherited values.
- P6: CI has a manual =workflow_dispatch= path while Pages publishing stays
  gated to explicit main-branch dispatches. Focused drift gates cover
  =CONTRACT_ORG= boolean predicates and org-elements alignment snapshots.
- P7: remaining stronger improvements are documented above and closeout gates

** Current P0-P7 Closeout Evidence

- P0: release/sync scope stays explicit: nested =languages/orgize= carries the
  implementation commit, while the parent repository should only stage the
  orgize gitlink for this lane.
- P1/P2: host-facing JSON query packets compile through the shared
  =OrgElementsIndexQuery= and =OrgElementQueryPredicate= AST via
  =org_elements_index_query_from_json_str= and
  =Document::org_elements_index_query_packet_json=. The executable boundary is
  =orgize elements-query --packet <json-query-packet> <org-file>=, which
  returns the filtered org-elements index JSON without routing through the
  document-provider string query surface.
- P3/P4: namespace and property-provenance policy is fixed by the
  =org_element_namespace_and_property_provenance= snapshot, covering
  org-mode-owned headline records and orgize extension objects.
- P5: upstream drift remains guarded by =.data/org-mode/lisp/org-element.el=
  defconst extraction and the fetched =.data/org-mode/doc/org-guide.org=
  regression fixture.
- P6: =CONTRACT_ORG= boolean assertion failures now include a compact predicate
  summary so any/none failures can be diagnosed from the lint snapshot without
  reopening the source fixture.
- P7: closeout gates for this lane are =cargo fmt --check=, =cargo check=,
  =cargo test --lib=, and the full =integration_test= suite from the nested
  orgize checkout.
  must run against the real provider/runtime boundary:
  =cargo fmt --check=, =cargo check --message-format short=,
  =cargo test --lib=,
  =cargo test --test integration_test semantic_ast_projects_elements_bridge=
  and =cargo test --test integration_test=.
